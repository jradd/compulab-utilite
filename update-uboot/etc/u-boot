DEFAULT_IMG='aImage-3.17.0-rc6-l4'
DEFAULT_DTB='imx6q-sbc-fx6m.dtb'

DEFAULT_BOOT_ARGS='console=${console},${baudrate} root=${boot_fs} rw rootwait ${video}'

UBOOT_READONLY='baudrate stderr stdin stdout'
UBOOT_RESERVED='ethaddr eth1addr ethact nandargs nandroot nandboot nandrootfstype run_eboot'

BOOT_ARGS="cgroup_disable=memory"

